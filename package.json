{
  "name": "site-studio-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "medusa build",
    "migrate": "medusa db:migrate",
    "start": "medusa db:migrate && medusa start --host 0.0.0.0 --port ${PORT}",
    "dev": "medusa develop"
  },
  "dependencies": {
    "@medusajs/framework": "^2.0.0",
    "@medusajs/medusa": "^2.0.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@medusajs/test-utils": "^2.0.0",
    "jest": "^29.7.0"
  }
}
