# ---------- Local development ONLY ----------
# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/medusa_db

# Redis (optional for local; only set if you actually run Redis)
REDIS_URL=redis://localhost:6379

# Secrets (replace in your real .env, never commit real values)
JWT_SECRET=replace-with-a-long-random-string
COOKIE_SECRET=replace-with-a-long-random-string

# CORS = where your UIs run in the browser (origins, not API ports)
# Storefront dev (Next.js)
STORE_CORS=http://localhost:3000
# Admin dev (Medusa Admin dev server)
ADMIN_CORS=http://localhost:7001

# Only needed when the Admin runs as a separate dev server
# (lets Admin talk to your local backend)
MEDUSA_BACKEND_URL=http://localhost:9000

# Server (local only)
PORT=9000
NODE_ENV=development
